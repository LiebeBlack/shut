# Platform-specific implementation notes for the sshub.platform_layer package.
# The package is intentionally a single module (__init__.py) to keep imports
# flat and startup fast on low-power machines.
